# 작업 기록

두 공급사 연동을 만들면서 내린 판단과, 그 판단이 흔들렸던 지점을 날짜별로 남긴다.
결론만 적으면 나중의 내가 왜 그랬는지 모른다.

---

## Day 1 — 2026-09-01

스펙 읽고, 설계 결정 몰아서 끝내고, 뼈대만 세웠다. 기능 구현은 한 줄도 안 했다.

### 오늘 만든 것

```
src/main/java/io/github/jys0615/stayport/
  domain/          StayOffer, Price, DailyRate, SearchQuery, SupplierId
  application/port/ SupplierAdapter, SupplierResult, CatalogResult, FailureType,
                    SupplierStay, SupplierRoomType
  infra/           StayportProperties
  {api,adapter/*,mock}/  package-info.java 만
src/main/resources/  application.yml, application-mock.yml
docs/design.md
```

`./gradlew build` green. 테스트는 Initializr가 만들어 준 컨텍스트 로딩 하나뿐이라 사실상 컴파일
확인이다.

### 요금 모델을 두 번 뒤집었다

처음엔 `nightlyRate`(net)와 `taxAmount`를 표준 모델 필드로 올릴 생각이었다. A가 그렇게 주니까
더 풍부한 모델처럼 보였다.

B 스펙 다시 읽고 접었다. B는 세금 금액을 아예 안 준다. `taxIncluded: true`만 알려준다. 그러면
B 상품의 `taxAmount`는 영원히 null인데, 세금을 화면에 찍는 클라이언트 입장에서 null과 0원은
구분이 안 된다. 없는 값을 계약의 1급 시민으로 만들면 조용히 틀린 화면이 나온다.

그래서 공통 필드는 "기간 전체 총액·세금 포함" 하나로 줄였다.

그런데 그렇게 하니 A의 날짜별 분해가 통째로 버려진다. 여기서 한 번 더 멈췄다. 버리면 계약은
깔끔한데 요금 분해는 역산이 안 된다. `dailyBreakdown`을 optional로 남기고 "있으면 날짜별 gross를
보장, net·tax는 주는 곳에서만"으로 의미를 고정했다. `DailyRate.grossOnly()`와 `decomposed()`
두 팩토리로 갈라 놓은 게 그 결과다.

### MVC + WebClient

전면 리액티브는 이 워크로드에 과하다. 검색 한 건이 부르는 외부 API가 공급사당 한두 번이다.
그렇다고 순차로 부르면 지연이 그대로 더해진다. 외부 I/O만 `Mono.zip`으로 묶고 컨트롤러 경계에서
받아 동기 DTO로 내보내기로 했다.

가상 스레드도 봤다. 블로킹 코드 그대로 두고 스레드 비용만 낮추는 쪽이 읽기 쉽다. 근데 "두 공급사를
동시에 부른다"를 직접 조립해야 해서 병렬 구조가 코드에서 덜 보인다. 드러나는 쪽으로 갔다.

대가는 요청 하나가 최대 3.5초 서블릿 스레드를 잡는 것. 이걸 단점으로만 적다가 뒤집어 생각했는데,
타임아웃 값이 곧 스레드 점유 시간의 상한이다. 짧게 잡을 근거가 UX만이 아니라 용량에도 있다.
README에 두 근거 다 쓸 것.

### 나머지 판정

- **동기화 동시성** — 락으로 막으려다 방향 바꿈. 쓰는 주체가 동기화 하나뿐인데 락이 필요한 건
  동시 실행을 허용해서다. 진입점 하나로 만들고 이미 돌면 즉시 반환. UNIQUE 제약은 전제가 깨졌을
  때의 안전망. 단일 인스턴스 전제가 생겼고, 감추지 말고 설계 문서에 적기로.
- **검색 API 상태 코드** — 요청 오류만 400, 나머지 전부 200. 양쪽 다 죽어도 200이다. 제일 불편한
  결정이라 반대 논거를 먼저 적어봤다("아무것도 못 가져왔는데 200은 거짓말 아닌가"). 그래도 200인
  건 이 API의 실패가 공급사 단위로 부분적이기 때문. 전체를 5xx로 접으면 "A는 됐고 B만 죽었다"를
  쓸 자리가 없어진다.
- **파싱 오류 격리** — 응답 전체가 깨지면 공급사 실패, 개별 상품 하나면 그것만 스킵. 조용히 버리는
  게 더 무서워서 스킵 수를 센다. 이것 때문에 `SupplierResult.Success`가
  `(offers, skippedItems)`가 됐다.
- 기동 시 동기화 실패해도 앱은 뜬다 / 내부 식별자는 auto-increment Long 그대로 노출 /
  숙소·객실명은 매핑 스냅샷이 정본 / Mock은 같은 트리의 `mock` 프로파일.

### AI 활용

**설계 문서 리뷰** — 스펙을 같이 주지 않고 돌린 첫 리뷰에서 지적 7건. **5건 거부, 2건 수용.**

거부한 것들은 스펙과 대조하니 사실이 아니었다. "RatePlan 개념이 빠졌다"(두 공급사 다 그런 개념을
안 준다), "B도 날짜별 재고를 주는데 왜 min이냐"(B는 재고만 날짜별이고 요금은 총액), "페이징이
필요하다"(검색 조건에 페이징이 없다). 그럴듯한데 틀렸다.

수용한 2건은 진짜였다. 모든 공급사 호출에 `X-Api-Key`가 필요한데 계획에 인증이 아예 없었다.
그리고 타임아웃 예산이 모순이었다 — 호출 3초에 전체도 3초로 적어놓고 병렬이니 괜찮다고 넘어갔는데
병합·직렬화 시간이 예산 밖에 있었다. 전체를 3.5초로 고쳤다.

두 번째 리뷰는 스펙을 같이 줬고 오탐이 눈에 띄게 줄었다. 리뷰 품질이 입력에 비례한다. 이후로
"요구사항과 대조해서 확인하라"를 리뷰 요청의 기본형으로.

**중복 숙소 매칭에 로컬 임베딩 모델(ONNX)을 쓰자는 제안** → **거부.** 기술적으로 재밌고 실제로 이
문제에 맞는 접근이다. 거부 이유는 우선순위 — 선택 항목이 필수 기능과 재현성보다 앞설 수 없다.
모델 파일이 들어오면 clone 후 실행도 무거워진다. 매칭 신호와 자동 병합을 피한 이유는 설계 문서로
남긴다.

**Java냐 Kotlin이냐** → 제안은 Kotlin, 근거는 "팀 스택 추정"이었다. **결론은 받고 근거는 버렸다.**
추정은 근거가 아니다. 내가 채택한 근거는 설명 책임이다. 코드 어느 지점을 짚어도 왜 그렇게 썼는지
말할 수 있어야 하는데 그 확신이 있는 쪽이 Java다. record와 sealed interface로 표준 모델과
`SupplierResult`를 쓰면 Kotlin에서 얻으려던 간결함은 대체된다.

**문서를 다 쓰고 "사람이 쓴 것처럼 읽히는지" 다시 리뷰를 붙였다.** 이건 오늘 제일 뼈아팠다.
지적 4건 중 3건 수용, 1건 오탐.

- design.md가 아직 없는 동작을 완료형으로 단정하고 있었다. "`POST /internal/sync`로 수동
  재실행할 수 있다"고 써놨는데 컨트롤러가 없다. 계획을 사실처럼 쓴 것. 문서 첫머리에 구현 현황
  표를 넣고, 어느 구간이 코드보다 앞선 상태인지 밝히는 걸로 고쳤다.
- "ArchUnit으로 강제할 예정이다" 바로 다음 줄에 "어긋나면 빌드가 깨지게 만드는 쪽을 택했다"고
  써놨다. 같은 문단에서 시제가 뒤집힌다. 변명거리 없는 흠이다.
- 격언조 문장이 너무 많다는 지적. "예산은 지켜지는지 재보지 않으면 예산이 아니다" 류. 읽기 좋지만
  정보가 없다. 걷어냈다.
- 오탐 1건: "README에 두 근거를 같이 쓰기로"가 잘못된 참조라고 했는데, 타임아웃 근거는 README에
  들어가는 게 맞다. 리뷰어가 그 계획을 못 본 것.

### 막힌 것 / 미해결

**패키지 구조를 두 번 만들었다.** 처음엔 스케치대로 `domain/port`에 포트를 뒀다가 `application/port`로
옮겼다. 포트는 "도메인이 가진 것"이 아니라 "유스케이스가 필요로 하는 것"이라서다. `domain` 밑에
`SupplierAdapter`가 있으면 "도메인은 공급사를 모른다"는 선언과 정면으로 어긋난다.

옮기고 나서 빈 `domain/model`, `domain/port` 디렉터리가 남아 있는 걸 뒤늦게 봤다. git이 빈
디렉터리를 추적 안 해서 커밋엔 안 보였겠지만 로컬에선 헷갈릴 물건이라 지웠다.

**Mock 서버를 오늘 못 했다.** Day 1에 넣어뒀는데 설계 판정에 시간이 더 들었다. 어댑터 붙이는
순간 바로 필요하니 내일 제일 먼저.

**3.5초는 아직 확신 없다.** "체감 한계 + 스레드 점유 상한"이라는 논리로만 정했고 측정값이 아니다.
무응답 모드로 총 경과 시간 재보기 전까지 임시값.

**아직 안 정한 것:** `SupplierAdapter.fetchOffers()`가 청크를 이미 나눠 받는 시그니처인데, 청크를
누가 나눌지(유스케이스냐 어댑터냐)를 안 정했다. 공급사마다 한도가 다를 수 있어서 어댑터 쪽이
맞아 보이는데, 그러면 부분 실패 상태(PARTIAL)도 어댑터가 조립해야 한다. 내일 어댑터 시작 전에
결정.
