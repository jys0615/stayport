/**
 * 기술 관심사 — WebClient 구성, 설정 프로퍼티 바인딩.
 *
 * <p>운영 중 조정될 가능성이 있는 값(타임아웃, 청크 크기, 공급사 주소·키)은 코드 상수가 아니라
 * 여기의 프로퍼티로 들어온다. 값을 바꾸려고 재배포하는 상황을 만들지 않기 위해서다.
 */
package io.github.jys0615.stayport.infra;
